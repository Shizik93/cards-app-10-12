const initialState:any=[]

export const registrationReducers=(state:any=initialState,action:any)=>{
    switch (action.type){
        case '':{
            return state
        }
        default: return state
    }
}